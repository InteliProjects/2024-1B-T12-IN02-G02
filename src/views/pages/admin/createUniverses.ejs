<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Universe</title>
  <style>
    form {
      background: white;
      padding: 50px;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      
      max-width: 1100px;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    button {
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
    }

    button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <h1>Create a New Universe for Game ID: <%= gameId %></h1>
  
  <!-- Form to create a new universe -->
  <form action="/admin/createUniverse" method="post">
    <!-- Hidden input field to store the gameId -->
    <input type="hidden" name="gameId" value="<%= gameId %>">
    
    <!-- Input field for entering the universe number -->
    <input type="text" name="number" placeholder="Universe Number" required><br>
    
    <!-- Submit button to create the universe -->
    <button type="submit">Create Universe</button>
  </form>
</body>
</html>
