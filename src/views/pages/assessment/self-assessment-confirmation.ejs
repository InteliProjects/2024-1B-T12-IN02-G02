<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Self-Assessment Confirmation</title>
    <link rel="icon" href="/images/logoGlobalUnity.png" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            margin: 0;
            font-family: "Segoe UI", sans-serif;
            background-color: #f8f9fa;
        }
        .vh-100 {
            min-height: 100vh;
        }
        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .card-header {
            background-color: #1E5A6F;
            color: #fff;
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
    </style>
</head>
<body>
    <div class="d-flex justify-content-center align-items-center vh-100">
        <div class="card w-50">
            <div class="card-header text-center">
                <h2>Self-Assessment Confirmation</h2>
            </div>
            <div class="card-body text-center">
                <h4>Thank You for Completing Your Self-Assessment!</h4>
                <br>
                <img src="/images/check.png" class="img-fluid" alt="Checkmark" style="max-width: 200px;">
                <p class="mt-3">Your responses have been successfully submitted. We appreciate your time and effort in providing this valuable information.</p>
                <hr>
                <div class="mt-4">
                    <p><strong>Confirmation Details:</strong></p>
                    <p><strong>Date:</strong> <span id="confirmation-date"></span></p>
                </div>
                <div class="mt-4">
                    <p>Redirecting in <span id="countdown">5</span> seconds...</p>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Automatically set the confirmation date to today's date
        document.getElementById('confirmation-date').textContent = new Date().toLocaleDateString();

        function redirect() {
            window.location.href = '/collaboration-style';
        }
        
        let countdownElement = document.getElementById('countdown');
        let countdown = 5;
        
        let countdownInterval = setInterval(() => {
            countdown--;
            countdownElement.textContent = countdown;
            if (countdown <= 0) {
                clearInterval(countdownInterval);
            }
        }, 1000);

        setTimeout(redirect, 5000);
    </script>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

